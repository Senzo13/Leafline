import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  logo: {
    width: 320,
    height: 320,
    resizeMode: 'contain',
    alignSelf: 'center',
  },
});

export default styles;